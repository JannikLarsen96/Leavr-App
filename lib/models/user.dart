class User {
  int id;
  String appIdentifier;
  int maxVehicles;
  bool isDeleted;
  DateTime createdAt;
  User(this.id, this.appIdentifier, this.maxVehicles, this.isDeleted,
      this.createdAt);
}
